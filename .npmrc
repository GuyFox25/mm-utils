# Keep peer dependencies explicit — the layering rule depends on it.
auto-install-peers=false
# Every cross-package dependency is a peer dependency; surface missing ones loudly.
strict-peer-dependencies=false
